==================================
IDE / Editor / OS Specific
==================================
Visual Studio Code
.vscode/

JetBrains IDEs (PyCharm, WebStorm, etc.)
.idea/

macOS
.DS_Store

==================================
Backend (Python / FastAPI / Celery)
==================================
Python bytecode and cache
pycache/
*.pyc
*.pyo
*.pyd

Virtual environments
env/
venv/
.venv/

Python packaging artifacts
pip-wheel-metadata/
*.egg-info/
dist/
build/

Log files and environment variables
.log
.env
.env.
!.env.example

==================================
Frontend (Vue / Vite / Node)
==================================
Node.js dependency directory (very important!)
frontend/node_modules/

Build output directory
frontend/dist/

Vite cache
frontend/.vite/

Editor/IDE specific files within the frontend project
frontend/.vscode/

Log files generated by npm/yarn/pnpm
frontend/npm-debug.log*
frontend/yarn-error.log*
frontend/pnpm-debug.log*

==================================
Project Specific
==================================
Ignore all uploaded media files and processed results...
media/*

... but keep the empty 'media' directory in the repository.
To make this work, create an empty file named '.gitkeep' inside the 'media' folder.
!media/.gitkeep